startCommand:
  type: http
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    properties: {}
  customContainer:
    buildContext: .
    dockerfile: Dockerfile
    env:
      TRANSPORT: "http"
      PYTHONOPTIMIZE: "1"
      PYTHONUNBUFFERED: "1"
    port: 8000
  exampleConfig: {}