startCommand:
  type: http
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    properties: {}
  customContainer:
    buildContext: .
    dockerfile: Dockerfile
    env:
      TRANSPORT: "http"
    port: 8000
  exampleConfig: {}